# Conflux Desk 使用教程

> 多智能体协作 · 虚拟办公室——把子任务/子智能体的协作过程变成一间看得见的办公室：
> 角色坐工位、干活、走动交接、气泡对话，你还能随时暂停介入指挥。

---

## 一、启动方式

**方式 A · 本地服务器（推荐）**

```bash
cd conflux-desk
node server.js
```

浏览器打开 `http://localhost:8765/`。

**方式 B · 直接双击**

双击 `index.html` 即可（零依赖、无构建，全部本地运行）。

页面加载约 1 秒后自动开始播放项目的协作过程。

---

## 二、界面总览

| 区域 | 内容 |
| --- | --- |
| 顶栏左侧 | 🏢 品牌名 + **当前项目徽标**（如「订单跟踪功能 v1.2.0」） |
| 顶栏中间 | **阶段进度条**：调研 → 产品 → 设计 → 开发 → 测试 → 发布 → 交付，绿色 ✓ 为已完成，高亮为当前阶段 |
| 顶栏右侧 | **项目下拉框** · ⏸ 暂停/▶ 播放 · ↺ 重播 · 倍速 1x/2x/4x（默认 2x） |
| 办公室场景 | 两排工位，每个角色一台电脑（显示器有仿真桌面）、桌上有文件堆、人物头顶状态徽章 |
| 墙面 | 📌 白板显示当前阶段 · 🕐 挂钟显示项目内时间 |
| 左下角 | 📋 **协作动态**：全局事件流，点击某条可直达相关角色的详情 |

**人物状态徽章**：⌨️ 工作中 · 🚶 走动中 · 💬 交流中 · 📦 待交接 · ✅ 已完成 · 🎉 项目交付

**电脑屏幕三层内容**（随状态自动切换）：
- 待机：仿真桌面（📁 文件夹、飞书、微信、浏览器图标 + 任务栏）
- 工作中：角色专属窗口（写代码 / 文档 / 设计稿 / 测试清单 / 终端 / 图表…）
- 有人来交接：切换为聊天窗口

---

## 三、观看协作过程（基本用法）

1. 项目自动播放：角色完成工作后**起身沿过道走到**对方工位，人物**左上角弹出对话气泡**；
2. **缺陷回路**：测试发现问题会走到研发工位，弹出**红色 🐞 气泡**，研发修复后再走回去重新提测；
3. 桌面右上角的**文件堆**越多，说明该角色手上文件越多；
4. 全部完成后中央弹出交付横幅（交接次数 / 文件数统计）。

**播放控制**：
- ⏸/▶ 按钮，或**空格键**（输入框内打字时不会触发）；
- 倍速切换：1x / 2x / 4x，随时生效；
- ↺ 重播：状态归零重头再来。

---

## 四、查看详情（点击工位 / 人物 / 电脑）

点击任意角色的工位、人物或电脑 → 右侧弹出**工作台详情抽屉**：

- **顶部**：角色状态、当前任务与进度条、流水线链路（上游 → 我 → 下游）；
- **左栏 · 交接消息时间线**：该角色收到/发出的全部消息（红色=缺陷、琥珀=主管指令），气泡里说不完的都在附件里；
- **右栏 · 文件工作台**：按关系分三组——📥 输入（上游交付给我的）/ 📤 我的产出（我负责的）/ 📖 引用（只读），每个文件带版本号和状态徽章（草稿/修改中/待验收/已交付/有缺陷…）。

**联动操作**：
- **点消息** → 高亮它携带的附件文件（正向追溯）；
- **点文件** → 时间线自动滚动并高亮触发它的那条消息（反向追溯），同时打开文件详情；
- **文件详情（二级抽屉）**：左侧版本时间线（每版标注修改原因），右侧内容视图——代码看**红绿 diff**、文档看渲染正文、设计稿看线框图（v1/v2 可对比）、日志看终端输出；点不同版本即可切换对比；
- **ESC** 逐层关闭，或点击场景空白处。

---

## 五、人工介入（暂停 + @ 角色下指令）

发现某个角色"说错了"？暂停后直接指挥：

1. 按**空格**或顶栏 ⏸ 暂停（时间冻结，人物停在原地）；
2. 场景底部浮出**人工介入面板**：`@ [选择角色] + 输入指令 + 发送`；
   - 也可在文本里直接写「**@测试工程师** 这条用例必须覆盖」，系统自动识别并改投；
3. 消息以「**👔 主管**」身份送达目标角色：
   - 金色主管气泡 + 屏幕切聊天窗 + 屏幕闪烁；
   - 随后角色自动回复绿色气泡「**收到，马上调整 ✓**」；
   - 指令记入该角色消息时间线（琥珀色条目）和左下角协作动态，可回看；
4. 点击面板上的「**▶ 继续播放**」恢复项目运行。

> 典型用法：测试提交了 bug 但描述不对 → 暂停 → @测试工程师「复现步骤要带 order_id 样例」→ 看到确认回复 → 继续播放。

---

## 六、作为插件嵌入其他项目（主要用法）

把整套可视化**当作插件放进任何新项目文件夹**使用：

### 3 步嵌入

1. **复制**：把整个 `conflux-desk` 文件夹复制进你的项目（可改名如 `desk/`）；
2. **引入**：页面中加入宿主助手（就一个文件，无任何依赖）：

   ```html
   <div id="desk" style="height:700px"></div>
   <script src="./desk/desk-plugin.js"></script>
   ```

3. **挂载**：

   ```js
   const desk = Desk.mount({ container: '#desk' });   // 用内置演示数据
   // 或直接传你自己的项目数据：
   // Desk.mount({ container:'#desk', project: myProjectObj })
   ```

### Desk API（宿主驱动插件）

| 方法 | 作用 |
| --- | --- |
| `desk.loadProject(obj)` | **热替换**项目数据（roles/files/script），场景自动重建重播 |
| `desk.sendEvent(step)` | 推送**实时事件**：`{type:'handoff', from, to, text, kind, att}` 走动+气泡+文件交付；`{type:'work', role, task, dur}` 开工动画；`{type:'phase', name}` 换阶段 |
| `desk.pause()` / `resume()` / `replay()` | 播放控制 |
| `desk.intervene(roleId, text)` | 以「👔 主管」身份向指定角色下指令（自动暂停） |
| `desk.on(fn)` | 订阅插件上报：`desk:ready / desk:phase / desk:handoff / desk:done / desk:error` |

完整可运行示例见 `embed-demo.html`（模拟宿主页：按钮推事件、热替换数据、订阅上报）。

### 对接真实智能体系统

把 `desk.sendEvent(...)` 换成你们事件流的订阅回调即可：

```js
myAgentBus.on('task_handoff', e => desk.sendEvent({
  type: 'handoff', from: e.fromRole, to: e.toRole,
  text: e.summary, att: e.attachments,
}));
myAgentBus.on('file_updated', e => desk.sendEvent({
  type: 'handoff', from: e.by, to: e.to,
  text: `${e.file} 更新到 v${e.version}`,
  att: [{ fid: e.file, ver: e.version }], deliver: [{ fid: e.file, status: 'delivered' }],
}));
```

事件流落库后即可全程回放。iframe 天然样式隔离，不会污染宿主页面。

---

## 七、独立运行时管理多个项目（次要用法）

不嵌入、直接双击 `index.html` 时，也可以同时管理多个项目：

- **projectID = `projects/` 下的文件夹名**，自己命名（英文）即可；
- 复制 `projects/_template` → `projects/<文件夹名>/`，按注释填写 `project.js`；
- （可选）在 `projects/registry.js` 注册一行，项目出现在顶栏下拉框；
- 访问 `index.html?project=<文件夹名>` 切换；数据错误页面会显示中文校验提示，角色数变化工位自动重排。

---

## 七、快捷操作速查

| 操作 | 方式 |
| --- | --- |
| 暂停 / 继续 | 空格键，或顶栏按钮，或介入面板「▶ 继续播放」 |
| 打开角色详情 | 点击工位 / 人物 / 电脑 |
| 消息 ↔ 文件互溯 | 详情抽屉内互相点击 |
| 切换文件版本对比 | 文件详情左栏点版本 |
| 关闭抽屉 | ESC（先文件后角色）/ 点空白处 |
| 切换项目 | 顶栏下拉框，或 `?project=<文件夹名>` |
| 重播 | 顶栏 ↺ 重播 |

---

## 九、目录速览

```
conflux-desk/            ← 整个文件夹就是"插件"，复制即用
├── index.html           插件页面（独立打开 = 演示，被 iframe 加载 = 嵌入模式）
├── desk-plugin.js       ★ 宿主侧嵌入助手（宿主只需引入这一个文件）
├── embed-demo.html      宿主嵌入示例页
├── server.js            零依赖预览服务器（可选）
├── 使用教程.md          本文档
├── js/                  引擎（boot/data/scene/panel/sim/main/bridge）
└── projects/            内置演示项目 + 模板 + 注册表
```
